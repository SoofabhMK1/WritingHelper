[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning
# Coverage thresholds — floor, not target. Tighten over time.
# Configured via `pytest --cov` (pytest-cov).
# Tests can opt in via the markers below.